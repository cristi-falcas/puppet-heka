<% if @retries then -%>
    # retries configuration
    [<%= @full_name -%>.retries]
<% if @retries_max_jitter then -%>
    retries_max_jitter = "<%= @retries_max_jitter -%>"
<% end -%>
<% if @retries_max_delay then -%>
    retries_max_delay = "<%= @retries_max_delay -%>"
<% end -%>
<% if @retries_delay then -%>
    retries_delay = "<%= @retries_delay -%>"
<% end -%>
<% if @retries_max_retries then -%>
    retries_max_retries = "<%= @retries_max_retries -%>"
<% end -%>
<% end %>
